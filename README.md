# Team Diamonds — Website

## Run locally
```bash
npm install
npm run dev
```

## Survey → Google Sheets (optional)
1. Create a Google Cloud service account
2. Share your Google Sheet with the service account email
3. Copy `.env.example` to `.env.local` and fill values
